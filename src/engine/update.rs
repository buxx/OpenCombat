use crate::{message::*, types::*};

use super::Engine;

impl Engine {
    ///  - World pixel position according to movement
    ///  - ...
    pub fn update_entity(&self, _i: EntityIndex) -> Vec<Message> {
        // TODO
        vec![]
    }
}
